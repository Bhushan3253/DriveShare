spring.application.name=carpvt

spring.data.mongodb.uri=mongodb+srv://bhushan25developer_db_user:Bhushan1008@carrental.0a8kmk3.mongodb.net/car_rental?retryWrites=true&w=majority
spring.data.mongodb.database=car_rental
spring.data.mongodb.auto-index-creation=true

# Actuator Endpoints & Health Check
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

server.port=8081
server.address=0.0.0.0

# Payment UPI Settings
payment.upi.id=${PAYMENT_UPI_ID:9284940915@airtel}
payment.upi.name=Private Car Rental

# Cloudinary Settings
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dxpont2tq}
cloudinary.api-key=${CLOUDINARY_API_KEY:838569452822887}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:GVrRz8qsnW6TnJh3_OXFK66y3QM}

# Multipart File Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Secret Configuration
jwt.secret=${JWT_SECRET:carRentalSecretKeyForJwtAuthentication2026_Secure256BitKeyRequired}

# Application & Frontend URLs
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.mail.from=${MAIL_FROM:no-reply@driveshare.com}

# Resend HTTP REST API Configuration (Port 443 - Cloud Safe on Render)
resend.api.key=${RESEND_API_KEY:}
resend.from=${RESEND_FROM:DriveShare <onboarding@resend.dev>}

# Spring Mail / SMTP Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:bhushannagpure25@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fzfukmqjvjeeevdq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000